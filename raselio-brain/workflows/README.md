# Workflows — Raselio

Recurring automations on the Parker-MCP product surface (distinct from `schedules/`, which are repo-native cron routines). Empty at build time; `proposed/` holds dreaming-suggested workflows awaiting confirmation. Nothing can be registered here until Raselio exists as a brand in Parker.
