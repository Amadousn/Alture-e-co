# Audits — Raselio

The cadence layer: `[YYYY-MM]/` monthly cuts, `[YYYY-Q]/` quarterly cuts, each with `external/` for the competitor cuts. The newest audit is the account's present tense. `INDEX.md` is the generated map.
