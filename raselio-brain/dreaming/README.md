# Dreaming — Raselio

Populated by the nightly `/dream` routine: `runs/[YYYY-MM-DD]/` observations and `proposals/{pending,applied,dismissed}/` five-bucket proposals (context / skill / schedule / idea / open-loop). Empty at build time by design.
